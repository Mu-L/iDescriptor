# rsplay

`rsplay` is the Rust AirPlay receiver library used by iDescriptor. It owns the
receiver lifecycle, persistent pairing identity, DNS-SD registration, and
GStreamer audio/video playback.

TODO: add more stuff here
